{
  "dependencies": {
    "@tailwindcss/cli": "^4.1.10",
    "tailwindcss": "^4.1.10"
  },
  "devDependencies": {
    "daisyui": "^5.0.43"
  },
  "scripts": {
    "android": "dx serve --platform android --package mobile",
    "android:build": "dx bundle --platform android --package mobile --target aarch64-linux-android",
    "ios": "dx serve --platform ios --package mobile",
    "ios:build": "dx bundle --platform ios --package mobile --release",
    "back": "dx serve --package web",
    "back:test": "cargo test",
    "front": "cd ./frontend-react/counted && npm run dev",
    "front:test": "cd ./frontend-react/counted && npm run test"
  }
}
